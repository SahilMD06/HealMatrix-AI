# Development and CI dependencies. Layered on the core runtime only, so the
# test suite stays fast and does not need the PyTorch stack.

-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
mongomock-motor==0.0.34
ruff==0.8.4
faker==33.1.0
