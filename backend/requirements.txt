# ============================================================================
# HealMatrix AI - core runtime
#
# Everything needed to run the API, the simulator, the ML models and the report
# engine. Deliberately excludes the LLM/agent stack, which lives in
# requirements-ai.txt because sentence-transformers pulls PyTorch (~800 MB) and
# will not fit a 512 MB free-tier dyno.
#
#   pip install -r requirements.txt          # API + ML  (fast, ~90 s)
#   pip install -r requirements-ai.txt       # adds agents + RAG (slow, large)
# ============================================================================

# ---------- Web framework ----------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
websockets==14.1

# ---------- Database ----------
motor==3.6.0
pymongo==4.9.2

# ---------- Auth & security ----------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt is held at 4.0.1: passlib 1.7.4 reads the version via bcrypt.__about__,
# which bcrypt 4.1+ removed, producing a (trapped) traceback on every hash. 4.0.1
# still exposes it, so hashing is silent and identical in behaviour.
bcrypt==4.0.1
slowapi==0.1.9

# ---------- Async tasks ----------
celery==5.4.0
redis==5.2.1

# ---------- ML / numerics ----------
# numpy is held below 2.0: langchain 0.3.x requires numpy<2 on Python < 3.12,
# and pinning 2.x here is what broke the Docker build.
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.6.0
xgboost==2.1.3
scipy==1.14.1
joblib==1.4.2

# ---------- Reports ----------
reportlab==4.2.5
matplotlib==3.10.0
Pillow==11.0.0

# ---------- Storage & integrations ----------
cloudinary==1.41.0
httpx==0.28.1

# ---------- Observability ----------
structlog==24.4.0
prometheus-fastapi-instrumentator==7.0.2

# ---------- Utilities ----------
python-dateutil==2.9.0.post0
email-validator==2.2.0
tenacity==9.0.0
